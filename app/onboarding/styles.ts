import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  Heading: {
    fontSize: 20,
    fontWeight: "bold",
    textAlign: "center",
    marginVertical: 20,
  },
});

export default styles;